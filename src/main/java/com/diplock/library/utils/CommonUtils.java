package com.diplock.library.utils;

public class CommonUtils {
  // Esta clase es a modo de ejemplo.
  // Aquí se podría meter los métodos que puedan volverse genéricos (que se usen en distintos sitios),
  // para así no repetir código, siempre teniendo en cuenta que si vemos que esta clase se vuelve muy grande,
  // dividir en distintos archivos con la coletilla de Utils, que se dividan por la funcionalidad a la que van dirigidos.
}
